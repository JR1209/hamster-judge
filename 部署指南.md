# 🚀 仓鼠法官部署指南

将你的仓鼠法官部署到云端，让所有人都可以访问！

## 方式一：Streamlit Cloud 部署（推荐，免费）

### 📋 准备工作

1. **GitHub账号**
   - 如果没有，访问 https://github.com 注册一个
   
2. **Streamlit Cloud账号**
   - 访问 https://streamlit.io/cloud
   - 使用GitHub账号登录（直接关联）

### 🔧 部署步骤

#### 第一步：上传代码到GitHub

1. **创建GitHub仓库**
   ```bash
   # 在项目文件夹中打开终端
   cd /Users/z/Desktop/仓鼠法官
   
   # 初始化git仓库
   git init
   
   # 添加所有文件
   git add .
   
   # 提交
   git commit -m "Initial commit: 仓鼠法官裁决系统"
   ```

2. **在GitHub创建远程仓库**
   - 访问 https://github.com/new
   - 仓库名称：`hamster-judge`（或其他名称）
   - 设置为 Public（公开）
   - 不要勾选任何初始化选项
   - 点击 "Create repository"

3. **推送代码到GitHub**
   ```bash
   # 关联远程仓库（替换YOUR_USERNAME为你的GitHub用户名）
   git remote add origin https://github.com/YOUR_USERNAME/hamster-judge.git
   
   # 推送代码
   git branch -M main
   git push -u origin main
   ```

#### 第二步：在Streamlit Cloud部署

1. **访问Streamlit Cloud**
   - 打开 https://share.streamlit.io/
   - 点击 "New app"

2. **配置部署**
   - Repository: 选择你刚创建的仓库 `YOUR_USERNAME/hamster-judge`
   - Branch: `main`
   - Main file path: `hamster_judge.py`
   - App URL: 自定义你的网址（如 `hamster-judge`）

3. **高级设置（可选）**
   - 点击 "Advanced settings"
   - Python version: 3.9 或更高
   - 如果有密钥配置，可以在这里添加 Secrets

4. **部署**
   - 点击 "Deploy!"
   - 等待几分钟，应用会自动构建和部署

5. **完成！**
   - 部署成功后会得到一个网址，如：
   - `https://YOUR_APP_NAME.streamlit.app/`
   - 分享这个网址给任何人都可以访问

### 🔄 更新应用

当你修改代码后：

```bash
git add .
git commit -m "更新说明"
git push
```

Streamlit Cloud 会自动检测到变化并重新部署！

## 方式二：Hugging Face Spaces 部署（免费）

### 步骤

1. **注册Hugging Face**
   - 访问 https://huggingface.co/join
   - 注册账号

2. **创建Space**
   - 访问 https://huggingface.co/new-space
   - Space name: `hamster-judge`
   - License: Apache 2.0
   - SDK: Streamlit
   - 点击 "Create Space"

3. **上传文件**
   - 在Space页面点击 "Files and versions"
   - 上传以下文件：
     - `hamster_judge.py`
     - `requirements.txt`
   - 或使用git推送：
     ```bash
     git clone https://huggingface.co/spaces/YOUR_USERNAME/hamster-judge
     cd hamster-judge
     cp /Users/z/Desktop/仓鼠法官/* .
     git add .
     git commit -m "Initial commit"
     git push
     ```

4. **访问应用**
   - 网址：`https://huggingface.co/spaces/YOUR_USERNAME/hamster-judge`

## 方式三：Railway 部署（免费额度）

### 步骤

1. **访问Railway**
   - https://railway.app/
   - 使用GitHub登录

2. **新建项目**
   - 点击 "New Project"
   - 选择 "Deploy from GitHub repo"
   - 选择你的仓库

3. **配置**
   Railway会自动检测Streamlit应用并部署

4. **获取网址**
   - 在设置中可以生成公开访问网址

## 📱 部署后的配置

### 方法1：让用户自己配置API Key（推荐）

当前代码已经支持这个方式，用户在侧边栏输入自己的API Key即可使用AI功能。

**优点：**
- 无需你提供API Key
- 用户的使用不会消耗你的额度
- 更安全

**缺点：**
- 用户需要自己注册API

### 方法2：配置统一的API Key（不推荐）

如果你想让所有人直接使用AI功能，可以配置Streamlit Secrets：

1. **在Streamlit Cloud配置Secrets**
   - 进入你的应用管理页面
   - 点击 "Settings" → "Secrets"
   - 添加：
     ```toml
     QWEN_API_KEY = "your-api-key-here"
     ```

2. **修改代码使用Secrets**
   在 `hamster_judge.py` 中添加：
   ```python
   # 在文件开头添加
   import os
   
   # 在侧边栏API配置部分添加
   default_api_key = st.secrets.get("QWEN_API_KEY", "")
   if default_api_key:
       api_key = st.text_input("API Key", value=default_api_key, type="password")
   ```

**⚠️ 注意：** 这种方式会消耗你的API额度，请设置使用限制！

### 方法3：混合模式（推荐用于公开部署）

提供一个体验用的API Key（设置较低的限额），同时允许用户输入自己的Key：

```python
# 提供体验额度
demo_api_key = st.secrets.get("DEMO_API_KEY", "")

with st.expander("🔑 API配置说明"):
    st.info("""
    **两种使用方式：**
    1. 使用免费体验额度（每天限10次）
    2. 输入自己的API Key（无限制使用）
    """)
    
use_own_key = st.checkbox("我要使用自己的API Key")
if use_own_key:
    api_key = st.text_input("API Key", type="password")
else:
    api_key = demo_api_key
    st.warning("使用体验模式，今日剩余次数：X/10")
```

## 🎯 部署检查清单

- [ ] 代码推送到GitHub
- [ ] requirements.txt 包含所有依赖
- [ ] .gitignore 配置正确
- [ ] 在Streamlit Cloud创建应用
- [ ] 应用成功部署并可访问
- [ ] 测试模拟模式是否正常
- [ ] 测试AI模式是否正常（如果配置了）
- [ ] 检查手机端显示是否正常

## 🔍 常见问题

### Q: 部署失败怎么办？

A: 检查：
1. requirements.txt 是否包含所有依赖
2. Python版本是否兼容（建议3.9+）
3. 查看Streamlit Cloud的日志

### Q: 应用很慢？

A: Streamlit Cloud的免费版资源有限，考虑：
1. 优化代码性能
2. 减少不必要的计算
3. 升级到付费版

### Q: 如何限制API使用？

A: 可以：
1. 添加使用次数统计
2. 设置每日限额
3. 要求用户登录
4. 使用阿里云API的配额管理功能

### Q: 域名太长怎么办？

A: 可以：
1. 在Streamlit Cloud设置中自定义子域名
2. 购买自己的域名并配置CNAME
3. 使用短链接服务

## 📊 监控和分析

Streamlit Cloud提供：
- 访问量统计
- 应用健康状态
- 日志查看
- 资源使用情况

## 💰 费用说明

### Streamlit Cloud
- 免费版：1个公开应用
- 开发版（$20/月）：3个私有应用
- 团队版（$250/月）：无限应用

### API费用（如果配置了统一Key）
- 通义千问：约¥0.002/次
- 如果每天100人使用 = ¥0.2/天 = ¥6/月
- 建议设置每日总额度上限

## 🎉 部署成功后

分享你的应用：
- 朋友圈
- 社交媒体
- 技术社区
- 给朋友发链接

记得在README.md中更新访问链接！

---

祝部署顺利！🚀

